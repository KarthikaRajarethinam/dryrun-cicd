apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: microservice-a
  namespace: production
spec:
  imageRepositoryRef:
    name: microservice-a
  policy:
    alphabetic:
      order: asc
