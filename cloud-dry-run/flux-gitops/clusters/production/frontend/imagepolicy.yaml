apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: frontend
  namespace: production
spec:
  imageRepositoryRef:
    name: frontend
  policy:
    alphabetic:
      order: asc
